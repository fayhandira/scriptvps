#INSTALL PREMIUM SCRIPT JAY-PROJECT
```
apt update && apt upgrade -y && apt install -y wget screen && wget -q https://raw.githubusercontent.com/fayhandira/scriptvps/main/setup.sh && chmod +x setup.sh && screen -S setup ./setup.sh
```
